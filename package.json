{
  "name": "than-theology-library",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html dist/base.html && cp static-app.html dist/index.html && cp -a *.js *.webmanifest *.svg dist/"
  }
}
